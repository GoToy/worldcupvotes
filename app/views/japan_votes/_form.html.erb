<%= form_for(japan_vote) do |f| %>
  <% if japan_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(japan_vote.errors.count, "error") %> prohibited this japan_vote from being saved:</h2>

      <ul>
      <% japan_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :tournament_id %>
    <%= f.text_field :tournament_id %>
  </div>

  <div class="field">
    <%= f.label :ranking_id %>
    <%= f.text_field :ranking_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
